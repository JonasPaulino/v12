# Microservico de pagamentos
PORT=4200

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=123456
DB_DATABASE=database_v12

# Deve ser a mesma chave do backend principal
CHAVE_TOKEN=sua_chave_jwt_compartilhada_com_o_backend
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Deve ser o mesmo valor de PAYMENTS_SERVICE_TOKEN no back/.env
SERVICE_TOKEN=troque-este-token-interno
BACKEND_URL=http://localhost:4000
BACKEND_SERVICE_TOKEN=troque-este-token-interno
